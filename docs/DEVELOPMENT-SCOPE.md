# 개발 범위 요약: algo-quant

> 마지막 업데이트: 2026-01-11

## 개요

algo-quant는 **국내/해외 주식 + 암호화폐**를 대상으로 한 통합 퀀트 투자 자동화 에이전트입니다.

## 지원 자산 및 데이터 소스

### 1. 미국 주식
| 항목 | 내용 |
|------|------|
| 데이터 소스 | Financial Modeling Prep (FMP) API |
| 제공 데이터 | 일별 가격, 재무제표, 기업 프로필, 재무비율 |
| Rate Limit | 300 requests/min (Free tier) |
| 용도 | 팩터 모델링, 백테스팅 |

### 2. 한국 주식

#### 2.1 한국투자증권 (KIS)
| 항목 | 내용 |
|------|------|
| API 유형 | REST API (KIS Developers) |
| 인증 방식 | OAuth 2.0 (App Key/Secret + Access Token) |
| 제공 기능 | 시세 조회, 주문, 잔고 조회, 체결 내역 |
| Rate Limit | 초당 20건 (기본) |
| 장점 | 크로스 플랫폼 (Windows/Linux/Mac), 해외주식 지원 |
| 문서 | https://apiportal.koreainvestment.com |

```python
class KISClient:
    """한국투자증권 Open API 클라이언트"""
    - get_price(symbol)           # 현재가 조회
    - get_daily_prices(symbol, start, end)  # 일별 시세
    - get_balance()               # 잔고 조회
    - create_order(symbol, qty, price, side)  # 주문
    - get_order_history()         # 체결 내역
```

#### 2.2 키움증권
| 항목 | 내용 |
|------|------|
| API 유형 | REST API (Open API+) / COM (레거시) |
| 인증 방식 | App Key/Secret + Access Token |
| 제공 기능 | 시세 조회, 주문, 조건검색, 실시간 시세 |
| Rate Limit | 초당 5건 (기본) |
| 장점 | 조건검색 기능, 국내 최대 사용자층 |
| 문서 | https://openapi.kiwoom.com |

```python
class KiwoomClient:
    """키움증권 Open API 클라이언트"""
    - get_price(symbol)           # 현재가 조회
    - get_daily_prices(symbol, start, end)  # 일별 시세
    - get_balance()               # 잔고 조회
    - create_order(symbol, qty, price, side)  # 주문
    - search_condition(condition_id)  # 조건검색
```

### 3. 거시경제 지표
| 항목 | 내용 |
|------|------|
| 데이터 소스 | FRED (Federal Reserve Economic Data) |
| 주요 지표 | GDP, 실업률(UNRATE), 금리(FEDFUNDS), 수익률곡선(T10Y2Y) |
| Rate Limit | 120 requests/min |
| 용도 | 경기 체제 분류, 거시 시그널 |

### 4. 암호화폐

#### 4.1 Binance (글로벌)
| 항목 | 내용 |
|------|------|
| API 유형 | REST API + WebSocket |
| 인증 방식 | API Key/Secret (HMAC) |
| 제공 기능 | OHLCV, 주문, 잔고, 실시간 시세 |
| Rate Limit | 1200 requests/min |
| 지원 마켓 | USDT, BUSD, BTC 페어 |

```python
class BinanceClient:
    """Binance API 클라이언트"""
    - get_klines(symbol, interval, start, end)  # OHLCV
    - get_ticker(symbol)          # 현재가
    - get_balance()               # 잔고
    - create_order(symbol, qty, price, side)  # 주문
```

#### 4.2 Upbit (국내)
| 항목 | 내용 |
|------|------|
| API 유형 | REST API + WebSocket |
| 인증 방식 | JWT (Access Key/Secret) |
| 제공 기능 | OHLCV, 주문, 잔고, 입출금 |
| Rate Limit | 초당 10건 (주문), 분당 600건 (시세) |
| 지원 마켓 | KRW, BTC, USDT 페어 |

```python
class UpbitClient:
    """Upbit API 클라이언트"""
    - get_candles(market, interval, count)  # OHLCV
    - get_ticker(market)          # 현재가
    - get_balance()               # 잔고
    - create_order(market, side, volume, price)  # 주문
```

---

## 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           algo-quant Architecture                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │  FMP API    │ │  FRED API   │ │  KIS API    │ │ Kiwoom API  │       │
│  │ (US Stocks) │ │   (Macro)   │ │ (KR Stocks) │ │ (KR Stocks) │       │
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘       │
│         │               │               │               │               │
│  ┌──────┴───────────────┴───────────────┴───────────────┴──────┐       │
│  │                     Unified Data Layer                        │       │
│  │   (Preprocessor, Cache, Rate Limiter, Error Handler)         │       │
│  └──────────────────────────────┬───────────────────────────────┘       │
│                                 │                                        │
│  ┌─────────────┐ ┌─────────────┐│┌─────────────┐ ┌─────────────┐       │
│  │ Binance API │ │  Upbit API  │││Factor Models│ │   Regime    │       │
│  │  (Crypto)   │ │  (Crypto)   │││ (FF3, FF5)  │ │ Classifier  │       │
│  └──────┬──────┘ └──────┬──────┘│└──────┬──────┘ └──────┬──────┘       │
│         └───────────────┴───────┘       └───────────────┴───────        │
│                                         │                                │
│                              ┌──────────┴──────────┐                    │
│                              │   Strategy Engine   │                    │
│                              │ (Factor + Regime)   │                    │
│                              └──────────┬──────────┘                    │
│                                         │                                │
│                    ┌────────────────────┼────────────────────┐          │
│                    ▼                    ▼                    ▼          │
│             ┌──────────────┐   ┌──────────────┐   ┌──────────────┐     │
│             │  Backtester  │   │  KIS Order   │   │ Binance/Upbit│     │
│             │  (Research)  │   │ (KR Stocks)  │   │   (Crypto)   │     │
│             └──────────────┘   └──────────────┘   └──────────────┘     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 개발 Phase 요약

### Phase 1: Data Infrastructure (현재)
| 구성요소 | 상태 | 비고 |
|----------|------|------|
| Base Client | ✅ 완료 | Rate limiter, 재시도 로직 |
| FMP Client | 🔄 진행중 | 미국 주식 데이터 |
| FRED Client | ⏳ 대기 | 거시경제 지표 |
| KIS Client | ⏳ 대기 | 한국투자증권 |
| Kiwoom Client | ⏳ 대기 | 키움증권 |
| Binance Client | ⏳ 대기 | 글로벌 암호화폐 |
| Upbit Client | ⏳ 대기 | 국내 암호화폐 |
| Preprocessor | ⏳ 대기 | 데이터 전처리 |
| Cache System | ⏳ 대기 | 로컬 캐싱 |

### Phase 2: Factor Modeling
- CAPM, Fama-French 3/5팩터 모델
- 한국 시장 팩터 데이터 수집 (SMB, HML 등)

### Phase 3: Regime Classification
- FRED 기반 경기 체제 분류
- 한국 거시경제 지표 추가 (한국은행 API)

### Phase 4: Strategy Development
- 팩터 기반 자산 배분
- 체제 적응형 포트폴리오
- 주식-암호화폐 혼합 전략

### Phase 5: Backtesting
- 백테스팅 엔진
- 성과 지표 (Sharpe, MDD 등)
- Walk-forward 분석

### Phase 6: Production (Live Trading)
- 한국투자증권/키움증권 실거래 연동
- Binance/Upbit 실거래 연동
- 자동 리밸런싱

---

## API 인증 설정

```yaml
# config/api_keys.yaml
fmp:
  api_key: "YOUR_FMP_API_KEY"

fred:
  api_key: "YOUR_FRED_API_KEY"

kis:
  app_key: "YOUR_KIS_APP_KEY"
  app_secret: "YOUR_KIS_APP_SECRET"
  account_no: "YOUR_ACCOUNT_NUMBER"
  account_code: "01"  # 01: 위탁, 02: 연금저축
  is_paper: true      # true: 모의투자, false: 실전투자

kiwoom:
  app_key: "YOUR_KIWOOM_APP_KEY"
  app_secret: "YOUR_KIWOOM_APP_SECRET"
  account_no: "YOUR_ACCOUNT_NUMBER"
  is_paper: true

binance:
  api_key: "YOUR_BINANCE_API_KEY"
  api_secret: "YOUR_BINANCE_SECRET"
  testnet: true       # true: 테스트넷, false: 실거래

upbit:
  access_key: "YOUR_UPBIT_ACCESS_KEY"
  secret_key: "YOUR_UPBIT_SECRET_KEY"
```

---

## 디렉토리 구조

```
src/
├── data/
│   ├── __init__.py
│   ├── base_client.py       # 공통 HTTP 클라이언트
│   ├── fmp_client.py        # FMP API (미국 주식)
│   ├── fred_client.py       # FRED API (거시경제)
│   ├── kis_client.py        # 한국투자증권 API
│   ├── kiwoom_client.py     # 키움증권 API
│   ├── binance_client.py    # Binance API
│   ├── upbit_client.py      # Upbit API
│   ├── preprocessor.py      # 데이터 전처리
│   └── cache.py             # 캐싱 시스템
├── factors/                 # 팩터 모델
├── regime/                  # 체제 분류
├── strategy/                # 투자 전략
├── backtest/                # 백테스팅
└── execution/               # 실거래 (Phase 6)

tests/
├── data/
│   ├── test_fmp_client.py
│   ├── test_fred_client.py
│   ├── test_kis_client.py
│   ├── test_kiwoom_client.py
│   ├── test_binance_client.py
│   └── test_upbit_client.py
└── ...
```

---

## 우선순위 및 의존성

```
1. Base Client (완료)
   ↓
2. FMP Client (진행중) ─────────────────┐
   ↓                                     │
3. FRED Client                           │
   ↓                                     │
4. KIS Client ─────┬───> Preprocessor ───┴──> Factor Models
   ↓               │
5. Kiwoom Client ──┤
   ↓               │
6. Binance Client ─┤
   ↓               │
7. Upbit Client ───┘
```

---

## 참고 문서

| 문서 | URL |
|------|-----|
| FMP API | https://site.financialmodelingprep.com/developer/docs |
| FRED API | https://fred.stlouisfed.org/docs/api/fred/ |
| 한국투자증권 | https://apiportal.koreainvestment.com |
| 키움증권 | https://openapi.kiwoom.com |
| Binance API | https://binance-docs.github.io/apidocs |
| Upbit API | https://docs.upbit.com |
